version: "2"
sql:
  - engine: "postgresql"
    queries: 
      - "./postgres/sqlc/queries.sql"
    schema: "./postgres/migrations"
    gen:
      go:
        package: "repository"
        out: "./postgres/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true